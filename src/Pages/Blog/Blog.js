import PageHeader from "../../Components/PageHeader";

const BlogPage = () => {
    return (
        <PageHeader>
            Blog Page
            Create blogs in Markdown and add them on react
        </PageHeader>
    );
}

export default BlogPage;
